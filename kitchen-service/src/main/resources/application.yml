server:
  port: 8083

spring:
  application:
    name: kitchen-service
  datasource:
    url: jdbc:mysql://localhost:3306/kitchen_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: m@noj&7263hi92
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  activemq:
    broker-url: vm://localhost?broker.persistent=false

camunda.bpm.client:
  base-url: http://localhost:8081/engine-rest
  subscriptions:
    kitchen-preparation:
      variable-names:
        - orderId
        - item
